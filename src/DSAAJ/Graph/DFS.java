package DSAAJ.Graph;

public class DFS {
    /* // Template for depth-first search (Pseudo code)
    void dfs(Vertex v) {
        v.visited = true;
        for each Vertex w adjacent to v {
            if (!w.visited) {
                dfs(w);
            }
        }
    }
    */
}
